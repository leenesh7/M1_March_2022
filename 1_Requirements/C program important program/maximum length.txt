#include<stdio.h>
#include<stdlib.h>

int main()
{
 char str1[101],str2[102];
 scanf("%s %s",str1,str2);
 int len1 = strlen(str1);
 int len2 = strlen(str2);
 if(len1>=len2)
 {
     printf("%s\n",str1);
 }
 if(len2>=len1)
 {
     printf("%s\n",str2);
 }
 return 0;
}