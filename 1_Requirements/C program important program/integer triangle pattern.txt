#include<stdio.h>
#include<stdlib.h>

int main()
{
 int N,row,col;
 scanf("%d",&N);
 for(row=1;row<=N;row++)
 {
     for(col=1;col<=row;col++)
     {
         printf("%d ",col);
     }
     printf("\n");
 }
 return 0;
}